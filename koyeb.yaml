name: p4-satisfacao
services:
  - name: backend
    dockerfile: backend/Dockerfile
    ports:
      - port: 5000
  - name: frontend
    image: node:20-alpine
    build_command: cd frontend && npm install && npm run build
    run_command: cd frontend && npx serve -s dist -l 80
    ports:
      - port: 80